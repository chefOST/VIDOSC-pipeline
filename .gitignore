# Upstream VidOSC checkout, if placed inside this repo instead of alongside it.
# The canonical location is a sibling directory -- see README.md.
/VidOSC/

# Checkpoints — too large for git (~127 MB each); download from Google Drive (see README.md)
checkpoints/
*.ckpt

# Extracted features — generated by extract_features.py
data/
feats/
*.pth.tar
*.zip

# Virtualenvs
.venv/
venv/

# W&B run logs — auto-generated
wandb/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# macOS
.DS_Store

# Secrets
.env

# Conda env (installed locally, not committed)
*.tar.bz2
*.conda
